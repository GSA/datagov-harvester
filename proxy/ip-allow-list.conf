# Basic Auth allowlist
#
# Add one IPv4 address or network per line, followed by the value 1:
#   203.0.113.25/32 1;  allows one address
#   203.0.113.0/24  1;  allows the whole network
# An address without a prefix, such as 4.4.4.4, is also accepted.
# IPv6 addresses use the same CIDR format.
#
# Use a full-line comment for notes; inline comments are not supported.
# Blank lines and lines beginning with # are ignored.
173.79.114.71/32 1;

8.8.8.0/24 1;

4.4.4.4 1;

2601:140:8e02:efc0::daf/128 1;
