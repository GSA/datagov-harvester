import logging

logger = logging.getLogger("harvester")


# stub, TODO complete
def compare(compare_obj):
    """Compares records"""
    logger.info("Hello from harvester.compare()")

    return compare_obj
