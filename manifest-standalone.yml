# DEPLOY with cf push -f manifest-standalone.yml
applications:
- name: airflow-test
  disk_quota: 2G
  instances: 2
  memory: 2G
  docker:
    image: ghcr.io/gsa/airflow-test:development
  command: airflow standalone